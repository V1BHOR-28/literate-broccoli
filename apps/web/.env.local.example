# Copy to apps/web/.env.local (or export in your shell) and adjust as needed.
# Next.js only exposes vars prefixed with NEXT_PUBLIC_ to the browser.

# Base URL of the FastAPI backend. Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

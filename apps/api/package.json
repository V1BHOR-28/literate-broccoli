{
  "name": "@ai-pm/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload",
    "build": "python -m compileall .",
    "lint": "python -m compileall ."
  }
}
